PORT=3001
CORS_ORIGIN=http://127.0.0.1:5173

# Optional. Without it the station still works — pasting a link needs no key —
# but searching and importing a playlist are switched off.
YOUTUBE_API_KEY=
